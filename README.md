# CMPSC 473 PA1 Starter Repository

See `PA1_473_Fall26.pdf` for the assignment specification.

Directories:
- `pointer/` — Part I
- `build/` — Part II (Makefile intentionally incomplete)
- `debug/` — Part III (contains an intentional bug)
- `address-space/` — Part IV
- `memory-bugs/` — Part V (contains intentional bugs)
- `processes/` — Part VI

Each directory can be built independently with `make` once its Makefile is complete.
Do not commit generated executables or object files.
